apiVersion: v1
kind: ConfigMap
metadata:
  name: ssh-portfolio-info
  namespace: {{ .Values.namespace.name }}
  labels:
    app: ssh-portfolio
data:
  title: {{ .Values.configMap.title | quote }}
  description: {{ .Values.configMap.description | quote }}
  port: {{ .Values.configMap.port | quote }}

apiVersion: v1
kind: Service
metadata:
  name: ssh-portfolio
  namespace: ssh-portfolio
  labels:
    app: ssh-portfolio
spec:
  type: LoadBalancer
  selector:
    app: ssh-portfolio
  ports:
  - protocol: TCP
    port: 22
    targetPort: 22
    name: ssh
